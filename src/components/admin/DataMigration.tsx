"use client";

import { useState } from "react";
import { Database, Check, AlertTriangle } from "lucide-react";

export function DataMigration() {
    // Migration is no longer needed — all data lives in Supabase
    // and is managed via the SQL scripts + admin panel.
    // This component is kept as a placeholder and can be removed.
    return null;
}
